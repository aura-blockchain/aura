# AI Assistant

This directory contains tooling for the AI assistant service that interacts with the `aiassistant` module. It includes the GUI and supporting scripts used by operators.

## Notes

- On-chain functionality lives in `chain/x/aiassistant`.
- CLI usage is exposed under `aurad tx aiassistant` and `aurad query aiassistant`.

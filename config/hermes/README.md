# Hermes Config

This directory contains an example Hermes configuration for local or testnet use.

## Usage

1. Copy `config.toml.example` to `config.toml`.
2. Update RPC/GRPC endpoints, chain IDs, and key names for your environment.
3. Start Hermes with your updated config.

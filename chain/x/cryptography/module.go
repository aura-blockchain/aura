package cryptography

import (
	"context"
	"encoding/json"

	"cosmossdk.io/core/appmodule"
	"github.com/cosmos/cosmos-sdk/client"
	"github.com/cosmos/cosmos-sdk/codec"
	codectypes "github.com/cosmos/cosmos-sdk/codec/types"
	sdk "github.com/cosmos/cosmos-sdk/types"
	"github.com/cosmos/cosmos-sdk/types/module"
	"github.com/cosmos/cosmos-sdk/types/msgservice"
	"github.com/grpc-ecosystem/grpc-gateway/runtime"

	"github.com/aequitas/aura/chain/x/cryptography/keeper"
	"github.com/aequitas/aura/chain/x/cryptography/types"
	cryptoproto "github.com/aequitas/aura/proto/aura/cryptography/v1beta1"
)

var (
	_ module.AppModuleBasic     = AppModuleBasic{}
	_ appmodule.AppModule       = AppModule{}
	_ module.HasServices        = AppModule{}
	_ module.HasGenesis         = AppModule{}
	_ appmodule.HasBeginBlocker = AppModule{}
)

// AppModuleBasic defines the basic application module used by the cryptography module
type AppModuleBasic struct {
	cdc codec.Codec
}

// Name returns the cryptography module's name
func (AppModuleBasic) Name() string {
	return types.ModuleName
}

// RegisterLegacyAminoCodec registers the cryptography module's types on the LegacyAmino codec
func (AppModuleBasic) RegisterLegacyAminoCodec(cdc *codec.LegacyAmino) {}

// RegisterInterfaces registers the module's interface types
func (AppModuleBasic) RegisterInterfaces(registry codectypes.InterfaceRegistry) {
	msgservice.RegisterMsgServiceDesc(registry, &cryptoproto.Msg_ServiceDesc)

	registry.RegisterImplementations(
		(*sdk.Msg)(nil),
		&cryptoproto.MsgCreateKeyRotationSchedule{},
		&cryptoproto.MsgRotateKey{},
		&cryptoproto.MsgCreateThresholdScheme{},
		&cryptoproto.MsgSubmitThresholdSignatureShare{},
		&cryptoproto.MsgRegisterZKProofCircuit{},
		&cryptoproto.MsgSubmitZKProof{},
		&cryptoproto.MsgRegisterSecureEnclave{},
		&cryptoproto.MsgGenerateQuantumResistantKey{},
		&cryptoproto.MsgAddCertificatePin{},
		&cryptoproto.MsgUpdateParams{},
	)
}

// RegisterGRPCGatewayRoutes registers the gRPC Gateway routes for the cryptography module
func (AppModuleBasic) RegisterGRPCGatewayRoutes(clientCtx client.Context, mux *runtime.ServeMux) {
	// Register gRPC Gateway routes if needed
	// Note: RegisterQueryHandlerClient is generated by protobuf, uncomment when generated
	// cryptoproto.RegisterQueryHandlerClient(context.Background(), mux, cryptoproto.NewQueryClient(clientCtx))
}

// DefaultGenesis returns default genesis state as raw bytes for the cryptography module
func (AppModuleBasic) DefaultGenesis(cdc codec.JSONCodec) json.RawMessage {
	return cdc.MustMarshalJSON(types.DefaultGenesis())
}

// ValidateGenesis performs genesis state validation for the cryptography module
func (AppModuleBasic) ValidateGenesis(cdc codec.JSONCodec, _ client.TxEncodingConfig, bz json.RawMessage) error {
	var data cryptoproto.GenesisState
	if err := cdc.UnmarshalJSON(bz, &data); err != nil {
		return err
	}
	return types.ValidateGenesis(&data)
}

// AppModule implements an application module for the cryptography module
type AppModule struct {
	AppModuleBasic

	keeper *keeper.Keeper
}

// NewAppModule creates a new AppModule object
func NewAppModule(cdc codec.Codec, keeper *keeper.Keeper) AppModule {
	return AppModule{
		AppModuleBasic: AppModuleBasic{cdc: cdc},
		keeper:         keeper,
	}
}

// IsOnePerModuleType implements the depinject.OnePerModuleType interface
func (am AppModule) IsOnePerModuleType() {}

// IsAppModule implements the appmodule.AppModule interface
func (am AppModule) IsAppModule() {}

// InitGenesis performs genesis initialization for the cryptography module
func (am AppModule) InitGenesis(ctx sdk.Context, cdc codec.JSONCodec, data json.RawMessage) {
	var genesisState cryptoproto.GenesisState
	cdc.MustUnmarshalJSON(data, &genesisState)
	if err := am.initGenesis(ctx, &genesisState); err != nil {
		panic(err) // Genesis initialization must succeed
	}
}

// ExportGenesis returns the exported genesis state as raw bytes for the cryptography module
func (am AppModule) ExportGenesis(ctx sdk.Context, cdc codec.JSONCodec) json.RawMessage {
	gs, err := am.exportGenesis(ctx)
	if err != nil {
		panic(err) // Genesis export must succeed
	}
	return cdc.MustMarshalJSON(gs)
}

// ConsensusVersion implements AppModule/ConsensusVersion
func (AppModule) ConsensusVersion() uint64 { return 1 }

// RegisterServices registers module gRPC services using the keeper implementations.
func (am AppModule) RegisterServices(cfg module.Configurator) {
	cryptoproto.RegisterMsgServer(cfg.MsgServer(), keeper.NewMsgServerImpl(am.keeper))
	cryptoproto.RegisterQueryServer(cfg.QueryServer(), keeper.NewQueryServerImpl(am.keeper))
}

// initGenesis initializes the module's state from genesis
func (am AppModule) initGenesis(ctx sdk.Context, gs *cryptoproto.GenesisState) error {
	// Delegate to keeper's InitGenesis which has the complete implementation
	return am.keeper.InitGenesis(ctx, gs)
}

// exportGenesis exports the module's state to genesis
func (am AppModule) exportGenesis(ctx sdk.Context) (*cryptoproto.GenesisState, error) {
	// Delegate to keeper's ExportGenesis which has the complete implementation
	return am.keeper.ExportGenesis(ctx), nil
}

// BeginBlock performs module-specific logic at the beginning of each block
func (am AppModule) BeginBlock(ctx context.Context) error {
	sdkCtx := sdk.UnwrapSDKContext(ctx)

	// Process scheduled key rotations
	if err := am.keeper.ProcessScheduledRotations(sdkCtx); err != nil {
		am.keeper.Logger(sdkCtx).Error("failed to process scheduled rotations", "error", err)
	}

	// Check entropy health
	if err := am.keeper.CheckEntropyHealth(sdkCtx); err != nil {
		am.keeper.Logger(sdkCtx).Error("failed to check entropy health", "error", err)
	}

	// Cleanup expired certificate pins
	if err := am.keeper.CleanupExpiredPins(sdkCtx); err != nil {
		am.keeper.Logger(sdkCtx).Error("failed to cleanup expired pins", "error", err)
	}

	return nil
}

// EndBlock performs module-specific logic at the end of each block
func (am AppModule) EndBlock(ctx context.Context) error {
	return nil
}

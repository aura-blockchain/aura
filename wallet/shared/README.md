# Shared Multi-Chain Wallet

The multi-chain wallet implementation is maintained in the shared workspace to keep chain repos isolated.

Location:

`/home/hudson/blockchain-projects/shared/wallet/multi-chain-wallet`

If you need to run tests or build the shared wallet, do so from that shared path.

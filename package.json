{
  "name": "aequitas-aura",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "php-checks": "composer run test && composer run phpcs"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run php-checks"
    }
  }
}

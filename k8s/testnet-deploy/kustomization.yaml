apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aura

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - services.yaml
  - statefulset.yaml

commonLabels:
  app.kubernetes.io/name: aura
  app.kubernetes.io/part-of: aura-testnet
  environment: local
